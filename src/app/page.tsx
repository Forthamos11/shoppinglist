import Itementry from "./_components/Itementry";

export const dynamic = "force-dynamic";

export default async function HomePage() {
  
  
  // console.log(posts);

  return (
    <main>
      <Itementry />
    </main>
  );
}
